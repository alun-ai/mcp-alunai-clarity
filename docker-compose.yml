version: '3.8'

services:
  mcp-alunai-clarity:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-alunai-clarity
    ports:
      - "8000:8000"
    environment:
      - MEMORY_CONFIG_PATH=/app/data/config.json
      - SQLITE_DATA_PATH=/app/data/sqlite
      - PYTHONPATH=/app
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.alunai.clarity.version=2.0.0"
      - "com.alunai.clarity.implementation=sqlite"
      - "com.alunai.clarity.description=SQLite-based MCP server for memory persistence"